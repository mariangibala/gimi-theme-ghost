{{!< default}}
{{!-- The tag above means - insert everything in this file into the {body} of the default.hbs template --}}

{{!-- The main content area on the homepage --}}
{{!-- Loads partials/loop.hbs --}}

{{> loop

        twoColumns=true

        <!-- @if BUILD_TYPE='dev' -->
        showLoopPlaceholder=true
        <!-- @endif -->
        <!-- @if BUILD_TYPE='demo' -->
        showLoopPlaceholder=true
        <!-- @endif -->
        <!-- @if BUILD_TYPE='sell' -->
        showLoopPlaceholder=false
        <!-- @endif -->
}}
